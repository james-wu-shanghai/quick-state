接下来要为action增加一个autoMoveForward的配置标识。当action的autoMoveForward为true时，状态机在执行该action后进入下一个状态，而此时下一个状态的action有且只有一个，则状态机会自动执行下一个状态的action。
这个需求要求xml中对action有配置项，java代码中对autoMovingforward的属性进行判断，根据配置做自动推进处理。
底层功能实现好UT写在core module里，然后在sample module写一个例子